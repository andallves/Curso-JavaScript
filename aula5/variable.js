let name = 'Henry'; // String
name = 'Andrew';

console.log(name, 'both in 1984.');
console.log('In 2000', name, 'knows Mary.');
console.log(name, 'married Mary in 2012.');
console.log('Mary has a son with', name, 'in 2015.');
console.log("The", name, "'s son called Edward.");

// This is a variable declaration
let wifeName; // In the JavaScript it's allows to declare a variable without to assing a value it.
console.log(wifeName); // All the variable that haven't a value, 'underfined' will be assing it.

// This is a way to initializa a variable
wifeName = 'Mary';
console.log(wifeName)


// OBS:
// We can't create a variable using a reserved word;
// Variables needs to have a meaningful name;
// we can't start the variable name using a number;
// The variable name can't contain a hyphen '-' ou space ' ';
// Use a camelCase to declare a composite variable name;
// The variables are case-sensitive, there are diferente between a lowercase and uppercase;