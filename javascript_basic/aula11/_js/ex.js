// EXERCISE

// Get two inputs by user convertible to number
// Execute a sum with these values
// to finish prit the result in the window to user see

let number1 = Number(prompt('Enter a number: '));
let number2 = Number(prompt('Enter another number'));

let sum = number1 + number2;

alert(`Hey, tell you... the result to your sum é ${sum}`);