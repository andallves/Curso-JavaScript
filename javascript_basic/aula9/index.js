// PRIMITIVE DATA TYPES
// -> String, number, undefined, null, boolean.

// String
const name = 'Andre';
const lastName = "Alves";
const lastLastName = `Pereira`;

// number (int, float)
const number = 10;
const floatNumber = 10.39;

// undefined -> when isn't assing a value to variable
let studentName;

// null -> when we want to assing a value null to a variable
const studentLastName = null;

// boolean (true or false)
const boolean = true;

// To know what is the type of a variable
console.log(typeof floatNumber);

// To see the type and value, we can using this way.
console.log(typeof floatNumber, floatNumber);

