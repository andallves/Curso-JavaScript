// When we use let isn't possible redeclare a variable, but we can do it in when we use the var.
// let name = 'Andre';
// let name = 'Alves'; -> this is incorrect, probably will give an error.

var name = 'Andre';
var name = 'Alves'; // -> using the var this is correct and the code will run correctly

console.log(name)

// ECMAScript is a default syntax